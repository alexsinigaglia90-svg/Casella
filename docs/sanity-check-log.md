# Casella — Sanity Check Log

Append-only audit trail. One row per protocol run.

| Date | HEAD | Trigger | Status | Notes |
|---|---|---|---|---|
| 2026-04-24 | `b5991cc` | After Task 13 (per protocol) | 🟢 GREEN | 26 commits ahead of main; typecheck/tests/build all clean (26 tests passing, +5 vs Fase 0 baseline of 21); 2/2 migrations synced; 8 routes built incl. new `/api/pdok/*`. Mobile alignment: no escalations; PdokError class is a slight improvement (typed errors across mobile JSON boundary). gh CLI not installed → CI status not retrieved (branch is ~12 commits ahead of origin, push deferred until end of plan). |
| 2026-04-24 | `a6bd29e` | After Task 17 (per protocol) | 🟢 GREEN | 33 commits ahead of main (+7 since #1); typecheck/tests/build all clean; tests unchanged at 26 (no regressions); 3/3 migrations synced (added `0002_puzzling_rick_jones` for employees additions); new routes built: `/admin/medewerkers` (page) + `/api/admin/employees[/[id]]` (Route Handlers, ML-2 lock-in). Mobile alignment: ML-2 in-progress (Task 16 landed first set of admin Route Handlers; Tasks 20/25 still ahead). |
| 2026-04-24 | `41cbfd1` | After Task 20 (per protocol) | 🟢 GREEN | 38 commits ahead of main (+5 since #2); typecheck/tests/build all clean; tests unchanged at 26 (no regressions); 3/3 migrations synced (no new migration this batch); shell+toast+drawer landed. Mobile alignment: ML-2 in-progress — first consumer of Task 16 routes (drawer POSTs to `/api/admin/employees`, mapped PdokAddress→AddressInput); Task 25 still ahead. |
| 2026-04-24 | `cc34318` | After Task 25 (per protocol) | 🟢 GREEN | 45 commits ahead of main (+7 since #3); typecheck/tests/build all clean; tests unchanged at 26 (no regressions); 3/3 migrations synced; new routes built: `/admin/medewerkers/pending`, `/api/admin/employees/[id]/initiate-terminate`, `/api/admin/employees/[id]/cancel-termination`. Mobile alignment: ML-2 fully landed — all admin mutation surfaces are Route Handlers. CriticalConfirmDialog primitive ready for next destructive flows in 1.1b. |
| 2026-04-24 | `b9dc611` | After Task 31 (Plan 1.1a complete, pre-PR) | 🟢 GREEN | 51 commits ahead of main; all 6 packages typecheck clean (db/types/maps/auth/email/web); tests unchanged at 26 (db 7 + maps 5 + auth 14, +5 from Fase 0 baseline); build clean — 12 routes registered incl. all expected new ones. 3/3 migrations synced. pg_cron job `casella-employee-terminations` active (daily 5:00 UTC). Interactive browser smoke deferred to Alex (SSO login required). All Plan 1.1a checkpoints met. |
| 2026-04-24 | `5b1e3b2` | After Claude Design handoff integration (pre-PR) | 🟢 GREEN | 64 commits ahead of main (+13 since #4); all 6 packages typecheck clean; tests unchanged at 26; build clean — 15 routes incl. wizard drawer + Variant A list page + tweaks dock + admin shell. 4/4 migrations synced (added `0003_colorful_salo` for firstName+lastName). Design handoff fully landed: 4-step wizard with live preview, refined-table list page with sortable headers + status filter tabs + cookie-persisted prefs (density/avatars/statusVariant/columns) + drag-to-move tweaks dock. Layout overflow fix in admin+authed layouts (max-w-[1180px] mx-auto). Interactive smoke verified by Alex (drawer scroll, dock drag, theme toggle). |
| 2026-04-27 | `219e701` | After T8 + T9 (einde Chapter D, Plan 1.1b) | 🟢 GREEN | 17 commits ahead of main (+6 since #6 — wait, branch state changed: `main` rebase reset baseline; this is 17 commits since the 1.1a PR-merge HEAD `f0046ee`). Chapter D fully landed: T1 Playwright scaffold, T2 ML-1 design-tokens lift + DD-1 fg-* rename, T3 TD-6 ApiError shape, T4+T5 zod-catalog + cursor-SQL, T6 ML-5 theme-bootstrap, T7 DD-3 ThemeToggle arrow-keys, T8 TD-1 ESLint v9 flat-config + CI lint gate. All 7 packages typecheck clean. Tests: 29 passing total (types 3 + db 7 + maps 5 + auth 14 + web 0 (vitest exclude e2e)). Build clean — 15 routes (no new in D). Migrations 0000-0003 (no new in D — first new komt in T26 C-11). **ESLint: 0 errors, 0 warnings** (179 → 0 fix-loop in T8). Mobile alignment: ML-1 ✓ done (T2), ML-5 ✓ done (T6), ML-2 in-progress (route-handler discipline maintained; no new mutations), ML-3/ML-4/ML-6 no change (Fase 3 timing). Deferred-work bookkeeping: ML-1-FOLLOWUPS (open, 6 nits), ML-5-FOLLOWUPS (open, 2 nits), TD-1-FOLLOWUPS (open, 3 nits) — all NIT/LOW. Foundation lifted; ready for Chapter B (employee completion). Branch local-only (not pushed); push deferred until plan complete. Build-time warning: "Next.js plugin not detected in ESLint config" — informational; our flat-config uses @next/next directly and `pnpm lint` is the real gate. |
| 2026-04-27 | `4f69679` | After T14 (einde Chapter B, Plan 1.1b) | 🟢 GREEN | 24 commits ahead of main (+7 since #7). Chapter B fully landed: T10 B-3 manager-select hide (DD-4 abandoned), T11 B-1 EmployeeWizard mode-aware met diff-view + GET-endpoint + Employee/EmployeeWithAddress types in @casella/types, T12 B-2 intercepting-routes voor edit-drawer (Next 15 parallel + intercepting; pad-based URLs `/admin/medewerkers/[id]`; create-mode blijft `?new=1` search-param), T13 B-4 column-toggles spec + DD-5 close (impl was al in 1.1a Design handoff `5b1e3b2`). All 7 packages typecheck clean. Tests: 29 passing (types 3 + db 7 + maps 5 + auth 14 + web 0). Build clean — **17 routes** (+2: `/admin/medewerkers/[id]` + `/admin/medewerkers/(.)[id]` intercepted slot). Migrations 0000-0003 (geen DB-changes in B). **ESLint: 0 errors, 0 warnings**. Mobile alignment: ML-2 ✓ maintained (PATCH+GET routes als Route Handlers; consistent met 1.1a discipline). ML-1/ML-5 ✓ done; ML-3/ML-4/ML-6 no change. Deferred-work bookkeeping toegevoegd: B-1-FOLLOWUPS (open, 5 nits — stepper mode-awareness, country-cast, invite-email read-only, helper unit-tests, smoke debt) + B-2-FOLLOWUPS (open, 8 nits — double router.refresh, DTO duplicatie, lat/lng fallback, router.back durability, e2e selectors, smoke debt, polish). DD-4 abandoned (geen manager-role UI in 1.1b). DD-5 done (column-toggles + statusVariant impl in 1.1a, spec in T13). Manual smoke voor T11+T12 gedeferred — UX-validatie geblokkeerd op auth-fixture (Playwright spec `describe.skip`). Branch local-only; klaar voor Chapter C (AAA shell-chrome). |
| 2026-04-27 | `fd39c6a` | After T27 (einde Chapter C core, Plan 1.1b) | 🟢 GREEN | 37 commits ahead of main (+13 since #8). Chapter C core (T15-T26) fully landed in fallback-mode (no Claude Design mockup; archetype-referenced styling per spec C-0): T15 C-0 handoff marker, T16 C-1 TopBar skeleton (3-slot grid sticky `1180px`, `--surface-glass` chrome), T17 C-2 BreadcrumbProvider + per-page register-pattern via small `'use client'` `null`-return components (medewerkers, pending, employee-detail, profile), T18 C-6 EnvBadge sidebar→TopBar rightSlot, T19 C-3 ⌘K command-pill (palette open-state lifted to PaletteProvider, palette moved root→admin-only mount), T20 C-5 UserMenu dropdown (server-side displayName/email als prop, NIET useSession; `/admin/profile` placeholder route), T21 C-4 `?` shortcuts overlay (4 secties uit shortcuts-data, niet binnen input/textarea), T22 C-7 ContextActions slot in TopBar centerSlot (per-page useTopBarActions hook; primary/secondary/kebab kinds; medewerkers list-page-header inline buttons verwijderd), T23 C-8 BreadcrumbTrigger met cmdk popover + EmployeeListCacheProvider (parent-medewerkers + current-employee scopes), T24 C-9 command-palette mode-scoping (>/@/#/? prefix-chips; mixed/commands/employees/projects/help scopes), T25 C-10 global ⌘N quick-create (window-level handler, niet binnen inputs), T26 C-11 NotificationBell + audit-stream (DB migration 0004 voor `users.last_seen_audit_at`; GET /api/admin/audit/recent + POST /api/admin/audit/mark-seen — beide admin-only; bell met unread-dot + auto mark-seen-on-open). All 7 packages typecheck clean. Tests: **30 passing** (types 3 + db 8 (+1 new audit-list-recent signature test) + maps 5 + auth 14 + web 0). **20 routes** built (+1 /admin/profile in T20, +2 audit endpoints in T26 versus #8 baseline of 17). **ESLint: 0/0**. Migrations: **0000-0004** (5 rows; first new in 1.1b = 0004). Mobile alignment: ML-2 ✓ maintained — beide nieuwe audit endpoints zijn Route Handlers met admin-role check + apiError shape. ML-1/ML-5 ✓ done; ML-3/ML-4/ML-6 no change. Deferred-work updates: PROFILE-PAGE-STUB (open, T20). Provider-stack diepte: BreadcrumbProvider → EmployeeListCacheProvider → PaletteProvider → TopBarActionsProvider → QuickCreateProvider → ShortcutsOverlayProvider (6 levels). TopBar slot-prop API consistent over alle additions. Manual smoke gedeferred voor C-3..C-11 — UX-validatie geblokkeerd op auth-fixture. Branch local-only. Decision: continue met AAA-extensions T28-T32 (auto-save, coaching, pinned, server-search, presence) + T33 final wrap. |
| 2026-04-27 | `264c7e8` | After Chapter A (Fundament, Plan 1.6) | 🟢 GREEN | 5 commits ahead of main (na PR #9 spec/plan-merge): T1 schema-uitbreidingen + 0009 migration (employees +4 velden, projects +1, bonus_ledger +4, employer_statements +7), T2 nieuwe tabellen + 0010 migration (contracts/expense_claims/employee_change_requests/broadcasts/leave_balance_snapshots/bonus_config/care_package_ledger), T3 sidebar-fix + 14 placeholder-pagina's (employee 9 + admin 5; dode-link-regressie /verlof /contract /loonstroken voor employee weg), T4 notifications lib (16 employee + 6 admin event-types + DEFAULT_EMAIL_PREFS + enqueueNotification helper + 3 vitest cases), T5 email skeleton template (placeholder voor 14 nieuwe email-flows). All 8 packages typecheck clean. Tests: **35 passing** (+3 nieuwe web/notifications: types 3 + db 10 + maps 5 + auth 14 + web 3). Build clean — **38 routes** (+14 vs Fase 1.5: alle nieuwe placeholder employee/admin paths). ESLint: 0/0. Migrations: 0000-0010 (11 rows; +2 in Chapter A). Lokale db:migrate faalt op bekende journal-mismatch (zie project_state.md) — niet-blocking, CI applyt fresh. Klaar voor Chapter B (Verlof + Verzuim, 8 tasks). |
| 2026-04-27 | `d454c66` | After Chapter B (Verlof + Verzuim, Plan 1.6) | 🟢 GREEN | **14 commits ahead of main** (+8 vs Chapter A #16, +1 schema-upgrade pre-Chapter-B): pre-Chapter-B leave_requests-uitbreiding (0011 migration: type/hours/submittedAt/approvedAt/approvedBy/customPayload/attachmentStoragePath/availabilityStatus + leaveType nullable), T7 leave types-config (14 LeaveTypeKey met Wazo/BW-descriptions, 6 self-approve / 8 admin-approve) + balance-resolver (snapshot-cache + fallbackBalance heuristiek) + zod leaveSubmitSchema (+5 vitest cases), T8 /api/verlof/submit (per-type self-approve of admin-pending, audit + admin-email enqueue) + /api/verlof/cancel (eigenaarscheck, idempotent) + leave-status email-templates (leaveSubmittedAdminEmail + leaveDecidedEmployeeEmail), T9 /api/admin/verlof/[id]/approve + reject (requireAdmin, transactioneel, employee-decision-email), T10 Nmbrs leave-sync wiring (`@/lib/nmbrs/leave-sync.pushLeaveToNmbrs` met graceful skip op missing_credentials/no_nmbrs_id/no_nmbrs_mapping/not_approved + leaveTypeMap voor 12/14 types, hooked in approve-route best-effort), T11 employee /verlof UI (server-fetch requests+balances+weeklyHours, LeaveBalanceCards 4 saldo-types, LeaveForm 14-type select + per-type description/approval-mode hint, LeaveList met cancel-knop), T12 admin /admin/verlof queue (employee-name JOIN, ApproveCard met instant Goedkeuren + Afwijzen-via-dialog), T13 verzuim AVG-compliant (POST /api/verzuim/submit met type='sick' status='approved' self-approve + admin-email, POST /api/verzuim/recover met endDate=today + Nmbrs best-effort, sickSubmittedAdminEmail template, employee SickForm met radio-availability geen medische details, admin SickOverview read-only). All 8 packages typecheck clean. Tests: **45 passing** (+10 vs Chapter A: types 3 + db 10 + maps 5 + auth 14 + web 8 incl. 5 nieuwe leave/validation cases). Build clean — **49 routes** (+11 vs Chapter A: /api/verlof/submit, /api/verlof/cancel, /api/admin/verlof/[id]/approve, /api/admin/verlof/[id]/reject, /api/verzuim/submit, /api/verzuim/recover, /verlof, /verzuim, /admin/verlof, /admin/verzuim functional pages replacing placeholders). ESLint: 0/0. Migrations: 0000-0011 (12 rows; +1 in Chapter B: 0011_leave_requests_extended). Lokale db:migrate faalt op bekende journal-mismatch (Chapter A pattern) — niet-blocking, CI applyt fresh. Nmbrs SOAP `pushLeaveRequest` bestond al (1.3); leave-sync.ts nieuw als wrapper-met-mapping. AVG-compliant verzuim: geen medische velden, alleen availability + expected duration. Klaar voor Chapter C/D/E. |
| 2026-04-27 | `08aa406` | After Chapter C (Declaraties + Contract + Loonstroken, Plan 1.6) | 🟢 GREEN | **19 commits ahead of main** (+4 task-commits + 1 bugfix vs Chapter B #17). Chapter C fully landed: T15 Declaraties API (POST /api/declaraties submit + approve/reject + 8-categorie enum + per-category custom-fields + email-templates expenseSubmittedAdminEmail + expenseDecidedEmployeeEmail), T16 Declaraties UI (employee /declaraties list + /declaraties/nieuw single-step form + receipt-upload-stub + admin /admin/declaraties queue met BTW-split-dialog; EXPENSES-RECEIPT-UPLOAD deferred Fase 2), T17 Contracten (admin /admin/contracten upload-form + POST /api/admin/contracts met bonus-velden + GET /api/contract/[id]/download + employee /contract tijdlijn + sidebar-link; CONTRACTS-PDF-UPLOAD deferred Fase 2), T18 Loonstroken (getEmployeePayslips + getPayslipPdfBase64 API surface in @casella/nmbrs; NmbrsError 'not_implemented' stubbed; /api/loonstroken + /api/loonstroken/[year]/[month] proxy-routes; /loonstroken employee-pagina graceful empty-state; NMBRS-PAYSLIP-SOAP-IMPL deferred). All 8 packages typecheck clean. Tests: **40 passing** (types 3 + db 10 + maps 5 + auth 14 + web 8 — geen regressies). Build clean — **79 routes** (+30 vs Chapter B baseline van 49). ESLint: 0/0. Migrations: 0000-0011 (geen nieuwe in Chapter C — schema klaar in Chapter A/0010). Lokale db:migrate faalt op bekende journal-mismatch — niet-blocking, CI applyt fresh. 3 nieuwe deferred-work entries: NMBRS-PAYSLIP-SOAP-IMPL (open), CONTRACTS-PDF-UPLOAD (open), EXPENSES-RECEIPT-UPLOAD (open). Klaar voor Chapter D/E. |
| 2026-04-27 | `f2f1937` | After Chapter D (Bonus + Care Package + Werkgeversverklaring, Plan 1.6) | 🟢 GREEN | **25 commits ahead of main** (+6 vs Chapter C #18: 5 task-commits + 1 sanity). Chapter D fully landed: T20 bonus-formule helpers (`computeMonthlyBonus`: bruto-omzet (uren×rate) - direct (salaris+werkgeverslasten+vakantietoeslag/12+auto-stelpost) - indirect = nettowinst; `qualifiesForBaseline`: >=50% werkbare uren EN gem-tarief>=baseline; `determineApplicablePct`: rolling 12mnd qualified-count >=9 → abovePct; NL_HOLIDAYS hardcoded 2026-2027 in `working-hours-calendar.ts`) + 5 vitest cases, T21 `/api/cron/bonus-monthly-accrual` POST met CRON_SECRET-bearer-guard (backfill max 12mnd, per-employee per-project hours-aggregatie, project-rates skip null, bonus_config defaults bij ontbreken, qualified-month count uit bonus_ledger, clamp negative naar 0) + admin `/admin/bonus` 3-secties (per-medewerker overview-tabel, config-form `/api/admin/bonus/config` POST upsert, over-performance addendum-form `/api/admin/bonus/overperformance` POST → bonus_ledger type='adjustment'), T22 employee `/bonus` (YTD-accrual+adjustment+paid summary cards + history-tabel laatste 100 rows) + employee `/winstdeling` per-vennootschap-cards (Ascentra/Operis/Astra met annual-distribution + exit-payouts; empty-state copy bij geen rows) + `/api/bonus/saldo` + `/api/winstdeling/saldo` GET own-employee, T23 werkgeversverklaring auto-PDF (`@react-pdf/renderer` ADDED — only allowed install in Chapter D; 3 templates mortgage/rent/other met shared HeaderBlock/FooterBlock; auto-sign hardcoded admin-naam + Ascentra-employer-block; PDF-bytes als base64 in `employer_statements.generatedPdfPath` als stub voor Storage; download-route detecteert base64 via afwezigheid van `/`; status flow requested→delivered instant; `statementReadyEmployeeEmail` template) + employee `/werkgeversverklaring` page (radio-purpose-select + per-purpose conditionele velden + StatementList table met download-link). All 8 packages typecheck clean. Tests: **45 passing** (+5 nieuwe formula.test.ts cases vs Chapter C 40: types 3 + db 10 + maps 5 + auth 14 + web 13). Build clean — **86 routes** (+7 vs Chapter C 79: /api/cron/bonus-monthly-accrual, /api/admin/bonus/config, /api/admin/bonus/overperformance, /api/bonus/saldo, /api/winstdeling/saldo, /api/werkgeversverklaring, /api/werkgeversverklaring/[id]/download — alle replacing placeholder employee/admin pages /bonus, /winstdeling, /werkgeversverklaring, /admin/bonus). ESLint: niet-uitgevoerd (geen ESLint-changes). Migrations: 0000-0011 (geen nieuwe in Chapter D — schema klaar in Chapter A/0010). 3 nieuwe deferred-work entries: STATEMENTS-STORAGE-MIGRATION (open, base64-stub→Supabase Storage Fase 2), STATEMENTS-EMPLOYER-CONFIG (open, hardcoded werkgever-blok→multi-vennootschap config), STATEMENTS-ADMIN-SIGNATURE-CONFIG (open, hardcoded "Alex Sinigaglia"→admin-resolver). Klaar voor Chapter E. |
| 2026-04-27 | `e0dbfda` | Pre-PR: einde Plan 1.1b — alle 33 tasks compleet | 🟢 GREEN | **43 commits ahead of main** (+6 since #9). Chapter C extensions T28-T32 fully landed: T28 C-12 auto-save in EditWizard met If-Match optimistic-concurrency (PATCH route schrijft `auditMutation` action="employees.update_conflict" bij version-mismatch + 409 apiError; SavedIndicator pill met idle/saving/saved/error/conflict states; ConflictBanner met Herlaad-CTA → router.refresh), T29 C-13 shortcut-coaching tip-surfacing (localStorage tracker, Sonner toasts threshold-driven, opt-out via UserMenu, instrumented in palette/quick-create/shortcuts/context-actions), T30 C-14 pinned entities met DB migration 0005 user_pins + RLS policy (composite-PK, FK cascade, RLS via `app_current_user_id()` matching codebase convention) + GET/POST/DELETE pins API + sidebar FavoritesSection (top 5 + overflow) + detail-kebab Pin/Unpin via usePinToggle, T31 C-15 server-side tsvector search met DB migration 0006 (Dutch dictionary, GIN indexes, pg_trgm extension voor <3-char trigram fallback, ts_rank scoring) + GET /api/admin/search admin-only zod-validated + useServerSearch hook 300ms debounced + SearchPreview component (defer mount in palette — T31-FOLLOWUP) + EmployeesScopeContent merge cached/server-results, T32 C-16 presence-indicators **polling-only** (Realtime gedeferred — T32-REALTIME, geen @supabase/supabase-js install; in-memory Map cache met 30s TTL + heartbeat-via-GET; PresenceAvatarStack op /admin/medewerkers/[uuid] paths via usePathname regex). All 7 packages typecheck clean. **Tests: 32 passing** (types 3 + db 10 (+2 new: search-employees + audit-list-recent) + maps 5 + auth 14 + web 0). **24 routes** built (+4 vs #9: /api/admin/pins (×2), /api/admin/search, /api/admin/presence/[entityType]/[entityId]). **ESLint: 0/0**. **Migrations: 0000-0006** (7 rows; +3 in 1.1b: 0004 last_seen, 0005 pins, 0006 search-tsvector). Mobile alignment: ML-1 ✓ done (T2 4babdd8), ML-5 ✓ done (T6 6d150ed), ML-2 ✓ done (alle admin mutations Route Handlers), DD-1 ✓ done (T2), DD-3 ✓ done (T7 76c900b), DD-4 ✓ abandoned (T10 6cd5e5e), DD-5 ✓ done (1.1a 5b1e3b2 + T13 spec); ML-3/ML-4/ML-6 unchanged (Fase 3). Deferred-work delta: 11 closed/abandoned + 9 new entries (PROFILE-PAGE-STUB, FAVORITES-FULL-VIEW, PRESENCE-MULTI-USER-POLISH, T32-REALTIME, COACHING-DASHBOARD, ML-1-FOLLOWUPS, ML-5-FOLLOWUPS, TD-1-FOLLOWUPS, B-1-FOLLOWUPS, B-2-FOLLOWUPS — all NIT/LOW/MEDIUM, geen blockers). Provider-stack final: 7 levels (BreadcrumbProvider → EmployeeListCacheProvider → PaletteProvider → TopBarActionsProvider → QuickCreateProvider → ShortcutsOverlayProvider). Foundation hardened, Employees module compleet, AAA shell shipped (fallback-mode styling, designs-handoff komt mogelijk later voor visual refresh). **Klaar voor PR + merge.** Manual smoke is gedeferred voor wizard-edit/auto-save/presence-2-tab — auth-fixture nodig; e2e specs liggen klaar `describe.skip`. |
